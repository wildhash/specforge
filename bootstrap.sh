#!/usr/bin/env bash
# ─────────────────────────────────────────────────────────────────────────────
# SpecForge — GitHub Bootstrap Script
# Creates the wildhash/specforge repo, scaffolds all directories,
# writes every project file, and pushes to main.
#
# Usage:
#   export GITHUB_TOKEN=your_token
#   chmod +x bootstrap.sh && ./bootstrap.sh
# ─────────────────────────────────────────────────────────────────────────────
# See full script in chat file: bootstrap.sh
# All 30+ project files are embedded and pushed via GitHub Contents API
# ─────────────────────────────────────────────────────────────────────────────
echo "Download the full bootstrap.sh from the Complete.dev chat and run it."
echo "Repo target: https://github.com/wildhash/specforge"
